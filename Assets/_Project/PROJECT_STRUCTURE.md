# No AI Cultivation — Project Documentation

Last updated: 2025-07-21

## Project Overview

**Prison Realm: Alchemy Factory** — A Xianxia cultivation idle-factory game.
A modern chemist reincarnated into a cultivation world is banished to a Prison Realm.
Together with other prisoners, he revolutionizes alchemy to craft Realm-Breaking Pills and escape.

**60% Factory / 40% Cultivation-Combat-Exploration.**
Inspired by: Satisfactory, Arknights Endfield, Xianxia Novels (Martial Peak etc.)

---

## Assembly Architecture

```
Game.Core    (foundation — no game assembly dependencies)
  ↑
Game.Data    (depends on: Core)
  ↑
Game.Player  (depends on: Core, Data, Unity.InputSystem, Unity.Cinemachine)
  ↑
Game.Systems (depends on: Core, Data, Player, Unity.InputSystem)
  ↑
Game.UI      (depends on: Core, Data, Systems, Player, Unity.InputSystem, Unity.Properties)
  ↑
Game.Editor  (depends on: all game assemblies — Editor only)

Game.Tests   (depends on: Core, Data, Systems — Editor only, NUnit)
```

---

## Folder Structure

```
Assets/_Project/
│
├── Animations/              Reserved for animation clips and controllers
├── Audio/
│   ├── Music/               Background music tracks
│   └── SFX/                 Sound effects
├── Data/                    ScriptableObject asset instances (designer-editable)
│   ├── Crafting/            RecipeData + RecipeDatabase assets
│   ├── Enemies/             EnemyData assets (PrisonGuard, SpiritBeastT1, etc.)
│   ├── Items/
│   │   ├── Essences/        EssenceData assets (FireEssence, WaterEssence, etc.)
│   │   ├── Pills/           PillData assets (BasicPill, BreakthroughPill, etc.)
│   │   └── RawMaterials/    RawMaterialData assets (SpiritStone, IronOre, etc.)
│   ├── Machines/            MachineData assets (Furnace, Extractor, Pipe, etc.)
│   ├── Minor Worlds/        MinorRealmConfig assets
│   ├── Realm/               RealmDefinition assets (QiRefinement I-IX, etc.)
│   └── UI/                  UI data sources (HotbarDataSource, HUDDataSource, etc.)
├── Input/
│   └── AutoGenerated/       Unity Input System action maps (.inputactions)
├── Materials/
│   └── Prototyping Mats/    Prototype rendering materials, ghost materials
├── Models/
│   ├── OpenWorld/           Overworld environment models
│   └── second/              Additional models
├── Prefab/                  Prefab assets
│   ├── CraftingStations/    Machine prefabs (Furnace, Crusher, etc.)
│   ├── Items/               Item pickup prefabs
│   └── World/               World object prefabs
├── Scenes/                  Unity scene files (Universe, Grotto, MinorRealm, test)
├── Scripts/                 All C# source code (see below)
├── Shaders/                 Custom shader files
├── Sprites/                 2D art assets
│   ├── Biomes/              Biome/terrain sprites
│   ├── Characters/          Player and NPC sprites
│   ├── InnerWorld/          Inner world tile sprites
│   └── UI/                  UI icons and elements
├── Tests/
│   └── EditMode/            NUnit unit tests (Game.Tests assembly)
├── Textures/
│   └── Grass/               Terrain textures
└── Ui/                      UI Toolkit documents (.uxml/.uss)
    ├── Breakthrough/        Breakthrough confirmation panel
    ├── Building/            Build menu and hotbar
    ├── Crafting/            Crafting panel
    ├── Dialogue/            Dialogue overlay
    ├── Factory/             Machine inspect, dashboard, pipe connection
    ├── HUD/                 HUD layout (HP, Qi, Stamina, Buffs, Crosshair)
    ├── Inventory/           Inventory panel
    └── Pause/               Pause menu
```

---

## Scripts Folder Structure

```
Scripts/
├── Core/                        Foundation types shared by all assemblies
│   ├── CultivationBuffs.cs          Pill buff state tracking (static)
│   ├── GameEnums.cs                 All shared enums (MachineType, RealmLevel, etc.)
│   ├── GameEvents.cs                Core event bus (player state events)
│   ├── HealthSystem.cs              Health/damage mechanics (MonoBehaviour)
│   ├── IDamageable.cs               Damage interface (TakeDamage, IsDead)
│   ├── IInteractable.cs             Interaction interface (Interact)
│   └── IQiReceiver.cs               Qi receiver interface (AddQi)
│
├── Data/                        ScriptableObject class definitions
│   ├── DataTemplates/
│   │   ├── DialogueNode.cs          Dialogue tree nodes (ScriptableObject)
│   │   ├── EnemyData.cs             Enemy configuration (ScriptableObject)
│   │   ├── EssenceData.cs           Spirit essence data (extends ItemData)
│   │   ├── ItemData.cs              Abstract base for all items
│   │   ├── MachineData.cs           Machine configuration data
│   │   ├── NPCData.cs               NPC data template (ScriptableObject)
│   │   ├── OreVeinData.cs           Ore vein configuration data
│   │   ├── PillData.cs              Pill item data (extends ItemData)
│   │   ├── RawMaterialData.cs       Raw material data (extends ItemData)
│   │   ├── RealmDefinition.cs       Cultivation realm definitions
│   │   ├── RecipeData.cs            Crafting recipe definitions
│   │   ├── RecipeDatabase.cs        Recipe lookup database
│   │   └── SoundClip.cs             Sound asset data (ScriptableObject)
│   ├── GameDataEvents.cs            Data-layer event bus (factory events)
│   ├── HotbarDataSource.cs          Hotbar slot data (ScriptableObject)
│   ├── IInventory.cs                Inventory interface (AddItem)
│   ├── LootSystem.cs                Loot drop mechanics (static utility)
│   ├── MinorRealmConfig.cs          Minor realm generation config
│   └── SaveData.cs                  Save/load data structures
│
├── Player/                      Player-specific logic
│   ├── Camera/
│   │   ├── BuildElevation.cs        Build mode camera elevation
│   │   ├── CameraSystem.cs          3rd person ↔ Spirit Sense transitions
│   │   └── SpiritSenseCamera.cs     Spirit Sense overhead camera control
│   ├── Combat/
│   │   └── PlayerCombatController.cs Attack, dodge, abilities
│   ├── Interaction/
│   │   └── PlayerInteractor.cs      Interaction detection (OverlapSphereNonAlloc)
│   ├── Inventory/
│   │   └── PlayerInventory.cs       Dictionary-based item storage (ItemData)
│   ├── Movement/
│   │   └── PlayerMovement.cs        Camera-relative movement with Rigidbody
│   ├── PlayerPersistence.cs         Player data persistence across scenes
│   └── PlayerStats.cs               Cultivation state, Qi, realm, meditation
│
├── Systems/                     Game systems and world logic
│   ├── Audio/
│   │   ├── SoundEventTrigger.cs     Bridges GameEvents to SoundManager
│   │   └── SoundManager.cs          Audio singleton (SFX pool, music, volumes)
│   ├── Breakthrough/
│   │   └── BreakthroughSystem.cs    Cultivation rank-up mechanics
│   ├── Building/
│   │   ├── BuildGrid.cs             Grid management and occupation tracking
│   │   ├── LayerHelper.cs           Recursive layer assignment utility (static)
│   │   └── PlacementController.cs   Ghost preview, placement, rotation, removal
│   ├── Combat/
│   │   ├── EnemyAI.cs               NavMesh-based AI (Patrol → Detect → Chase → Attack)
│   │   └── EnemyLoot.cs             Loot dropping on enemy death
│   ├── Crafting/
│   │   └── CraftingSystem.cs        Manual crafting logic
│   ├── Essence/
│   │   └── EssenceSpawner.cs        Spirit essence world spawning (IInteractable)
│   ├── Factory/
│   │   ├── BaseMachine.cs           Core machine: input/output, timer processing
│   │   ├── IMachineConnectable.cs   Interface for pipe-connectable machines
│   │   ├── MachineInventory.cs      Shared item buffer with capacity limits
│   │   ├── Merger.cs                2-input / 1-output routing (2→1 combiner)
│   │   ├── OreVein.cs               World resource node (depletion + respawn)
│   │   ├── PipeConnectionVisualizer.cs  Visual pipe debugging (LineRenderer)
│   │   ├── QiConduit.cs             Qi power pole (conduit chain)
│   │   ├── QiNetwork.cs             Qi power grid manager (BFS, singleton)
│   │   ├── ResourceExtractor.cs     Auto-mines nearby OreVeins (4m radius)
│   │   ├── Splitter.cs              1-input / 2-output routing (round-robin)
│   │   ├── SpiritPipe.cs            Transport: connects machine outputs to inputs
│   │   └── StorageContainer.cs      Buffer storage between production steps
│   ├── Interaction/
│   │   ├── CraftingStation.cs       Manual crafting table interaction
│   │   ├── Portal.cs                Scene transition portal
│   │   ├── RealmPortal.cs           Realm-specific portal
│   │   ├── SceneEntryPoint.cs       Scene entry spawn point
│   │   └── SpiritEssence.cs         Collectible essence world object
│   ├── Pill/
│   │   └── PillBuffSystem.cs        Pill buff application and tracking
│   ├── Realm/
│   │   ├── BiomeZoneMap.cs          Biome zone mapping
│   │   ├── HeightmapBuilder.cs      Terrain heightmap generation
│   │   └── MinorRealmGenerator.cs   Minor realm procedural generation
│   └── Save/
│       ├── SaveSystem.cs            Core save/load logic (atomic JSON)
│       ├── SceneTransitionData.cs   Cross-scene data passing (static)
│       └── WorldState.cs            Persistent world state tracking (static)
│
├── Ui/                          All UI display and controllers
│   ├── Breakthrough/
│   │   └── BreakthroughController.cs    Breakthrough confirmation UI
│   ├── Building/
│   │   ├── BuildMenuDataSource.cs       Machine slot data struct
│   │   ├── HotbarController.cs          Hotbar slot UI management
│   │   └── MachineCatalogueController.cs Machine catalogue grid UI
│   ├── Combat/
│   │   ├── EnemyHealthBarUI.cs          Enemy health bar (world-space)
│   │   └── HealthBarUI.cs              Player health bar
│   ├── Crafting/
│   │   └── CraftingController.cs        Crafting panel controller
│   ├── DataSources/
│   │   ├── CraftingDataSource.cs        Crafting recipe slot data struct
│   │   ├── HUDDataSource.cs             HUD data binding (ScriptableObject + INotifyPropertyChanged)
│   │   └── InventoryDataSource.cs       Inventory slot data struct
│   ├── Dialogue/
│   │   └── DialogueUI.cs               Dialogue display with typewriter effect
│   ├── Factory/
│   │   ├── FactoryDashboardUI.cs        Factory dashboard (Qi status, machine list)
│   │   ├── MachineInspectUI.cs          Machine inspection panel (status, recipe, slots)
│   │   └── PipeConnectionUI.cs          Pipe connection HUD (source/dest selection)
│   ├── Inventory/
│   │   └── InventoryController.cs       Inventory panel management
│   ├── Pause/
│   │   └── PauseMenuController.cs       Pause menu logic
│   ├── Qi/
│   │   ├── HUDController.cs             HUD management (HP, Qi, Stamina, Buffs)
│   │   └── LiquidCircle.cs             Custom VisualElement for circular Qi bar
│   ├── Save/
│   │   └── SaveManager.cs              Save/load UI + persistence orchestration
│   ├── GameStateManager.cs              Panel state + cursor management (singleton)
│   ├── ResourcePickupNotificationUI.cs  Item pickup notifications (+N Item)
│   └── UIManager.cs                     Central UI init (BroadcastMessage to controllers)
│
├── Editor/                      Editor-only tools and inspectors
│   ├── CsvImportWindow.cs           Bulk item import from CSV
│   ├── EssencePainterWindow.cs      Biome-painted essence spawning
│   ├── InspectorAutoSetup.cs        Auto-wiring inspector utility (static)
│   ├── MinorRealmConfigEditor.cs    Custom inspector for MinorRealmConfig
│   ├── RealmProgressionLinker.cs    Realm progression chain linker (static)
│   └── RecipeDatabaseWindow.cs      Recipe validation + balance analysis
│
└── Tests/EditMode/              Unit tests (NUnit, Editor-only)
    ├── Game.Tests.asmdef            Test assembly definition
    ├── LootSystemTests.cs           Loot drop table tests
    ├── MachineInventoryTests.cs     MachineInventory capacity/stacking tests
    ├── RecipeDataValidationTests.cs RecipeData field validation tests
    ├── RecipeDatabaseLookupTests.cs Recipe lookup tests
    └── RecipeTestHelper.cs          Test helper utilities
```

---

## Script Reference

### Core (namespace: CultivationGame.Core)

| Script | Type | Purpose |
|--------|------|---------|
| **GameEnums.cs** | Enums | All shared enumerations: GameState, RealmSubStage, DaoType, DaoCategory, BiomeType, POIType, TerrainType, InnerWorldTileType, BuildingTerrain, BuildingRarity, SectRank, BuffType, **MachineType** (Furnace, Crusher, Mixer, Distiller, Condenser, PillPress, Storage, SpiritPipe, ResourceExtractor, Splitter, Merger) |
| **GameEvents.cs** | Static class | Core event bus for player state. Events: OnQiChanged, OnRealmChanged, OnStaminaChanged, OnInventoryChanged, OnMeditationToggled, **OnBuildModeToggled**, OnBuildLayerChanged, OnPauseStateChanged, OnPanelStateChanged, OnPlayerDodge, OnBuffStarted, OnBuffExpired, OnBreakthroughConfirmRequested, OnAttemptBreakthrough, OnRealmBreakthrough |
| **HealthSystem.cs** | MonoBehaviour | Manages entity health with maxHealth, currentHealth, isDead. OnHealthChanged event. Used by both Player and enemies |
| **IDamageable.cs** | Interface | Contract: TakeDamage(float amount, GameObject source), IsDead property. Implemented by player and enemies |
| **IInteractable.cs** | Interface | Contract for any world object the player can interact with. Single method: Interact(GameObject user) |
| **IQiReceiver.cs** | Interface | Contract for anything that can receive Qi. Single method: AddQi(double amount). Implemented by PlayerStats |
| **CultivationBuffs.cs** | Static class | Tracks active pill buff state: cultivationSpeedMultiplier, breakthroughBonus. Reset and apply methods for PillBuffSystem |

### Data (namespace: CultivationGame.Data)

| Script | Type | Purpose |
|--------|------|---------|
| **ItemData.cs** | ScriptableObject (abstract) | Base class for all items. Fields: itemName, description, icon, stackSize, rarity. Subclassed by EssenceData, RawMaterialData, PillData |
| **EssenceData.cs** | ScriptableObject | Spirit essence data (extends ItemData). Additional fields: qiValue, essenceColor, collectionEffect |
| **RawMaterialData.cs** | ScriptableObject | Raw material data (extends ItemData). Used for ores, herbs, minerals |
| **PillData.cs** | ScriptableObject | Pill item data (extends ItemData). Fields: pillTier, qiBoost, cultivationSpeedMultiplier, breakthroughBonus, buffDuration, maxDailyUses |
| **MachineData.cs** | ScriptableObject | Machine configuration. Fields: machineName, machineType, prefab, ghostPrefab, gridSize, buildCost, icon, processingSpeed, inputSlots, outputSlots, **qiConsumptionRate** |
| **OreVeinData.cs** | ScriptableObject | Ore vein configuration. Fields: resource (RawMaterialData), totalYield, yieldPerExtraction, respawnTimeSeconds |
| **RealmDefinition.cs** | ScriptableObject | Cultivation realm definitions. Fields: realmName, realmIndex, qiCapacity, baseQiRate, breakthroughSuccessRate, **spiritSenseRange**, baseDamage, baseDefense, nextRealm |
| **RecipeData.cs** | ScriptableObject | Crafting recipe. Fields: recipeName, description, inputs/outputs (RecipeIngredient[]), craftingDuration, requiredMachine, requiredRealm, successRate, qiCost |
| **RecipeDatabase.cs** | ScriptableObject | Recipe lookup database. Methods: GetRecipesForMachine(MachineType), GetRecipesForItem(ItemData) |
| **NPCData.cs** | ScriptableObject | NPC data template. Fields: npcName, portrait, startingDialogue (DialogueNode) |
| **DialogueNode.cs** | ScriptableObject | Dialogue tree node with text content and branching choice options |
| **EnemyData.cs** | ScriptableObject | Enemy configuration with stats (HP, damage, speed) and loot drop tables |
| **SoundClip.cs** | ScriptableObject | Audio configuration: clip, volume, pitch variance, 3D settings |
| **GameDataEvents.cs** | Static class | Data-layer event bus. Events: OnCraftingStarted/Completed/Failed, OnPillConsumed/EffectsApplied, OnMachinePlaced/Removed, OnBuildModeGhostStarted/Cancelled, OnMachineInteracted, OnMachineProcessingCompleted, OnMachineStalled, OnPipeConnected/Disconnected/Interacted, **OnQiNetworkChanged**, OnResourceExtracted |
| **HotbarDataSource.cs** | ScriptableObject | Manages 10 hotbar slots, fires OnSlotChanged events for UI binding |
| **IInventory.cs** | Interface | Contract for anything that stores items. Method: AddItem(ItemData, int amount). Implemented by PlayerInventory, MachineInventory |
| **LootSystem.cs** | Static class | Loot drop mechanics. GenerateLoot(LootDrop[]) returns List<LootResult> |
| **MinorRealmConfig.cs** | ScriptableObject | Minor realm generation configuration |
| **SaveData.cs** | Serializable classes | Save/load data structures: InventorySaveEntry, BuildingSaveEntry, PipeConnectionSaveEntry, MachineInventorySaveEntry, OreVeinSaveEntry |

### Player (namespace: CultivationGame.Player)

| Script | Type | Purpose |
|--------|------|---------|
| **CameraSystem.cs** | MonoBehaviour | Manages camera transitions between Cinemachine (3rd person) and SpiritSenseCamera (overhead). Build mode toggle independent of camera. Realm-based Spirit Sense zoom range |
| **SpiritSenseCamera.cs** | MonoBehaviour | Overhead camera for build/meditation mode. Pan (WASD), orbit (middle mouse), zoom (scroll). SetMaxZoom(float) for realm-based range scaling |
| **BuildElevation.cs** | MonoBehaviour | Build mode camera elevation control (Page Up/Down) |
| **PlayerStats.cs** | MonoBehaviour | Tracks player cultivation state (current realm, Qi). Handles meditation. Implements IQiReceiver. Fires GameEvents for Qi and realm changes |
| **PlayerMovement.cs** | MonoBehaviour | Camera-relative movement with Rigidbody physics. Sprint system with stamina drain/regen. Jump with ground detection |
| **PlayerInteractor.cs** | MonoBehaviour | Detects nearby IInteractable objects via OverlapSphereNonAlloc (pre-allocated buffer). Shows/hides interaction prompt. Triggers interaction on input action |
| **PlayerInventory.cs** | MonoBehaviour | Dictionary-based item storage (Dictionary<ItemData, int>). Implements IInventory. AddItem, RemoveItem, HasItem. Fires GameEvents.OnInventoryChanged |
| **PlayerCombatController.cs** | MonoBehaviour | Player combat: attack, dodge, abilities. Uses HealthSystem and IDamageable |
| **PlayerPersistence.cs** | MonoBehaviour | Handles player data persistence across scene transitions |

### Systems (namespace: CultivationGame.Systems)

| Script | Type | Purpose |
|--------|------|---------|
| **BuildGrid.cs** | MonoBehaviour | Grid management for building placement. Cell-based (configurable size). Tracks occupied cells. Terrain height snapping |
| **PlacementController.cs** | MonoBehaviour | Build mode controller. Ghost preview, grid snapping, rotation (90° steps), placement validation, inventory cost deduction. Wires MachineData to placed machines. Handles machine removal |
| **LayerHelper.cs** | Static class | Utility for recursive layer assignment on GameObjects and children. Used by PlacementController and SaveManager |
| **CraftingSystem.cs** | MonoBehaviour | Manual crafting logic. Checks recipe requirements, consumes inputs, produces outputs |
| **EssenceSpawner.cs** | MonoBehaviour | Spirit essence world spawning with respawn timers. Implements IInteractable |
| **IMachineConnectable.cs** | Interface | Contract for machine connectivity (SpiritPipe). Properties: InputInventory, OutputInventory, MachineData. Implemented by BaseMachine, ResourceExtractor, StorageContainer, QiConduit, Splitter, Merger |
| **BaseMachine.cs** | MonoBehaviour | Core machine component. Input/output MachineInventory, timer-based recipe processing. IsPowered set by QiNetwork. ProcessingProgress (0-1). IInteractable + IMachineConnectable |
| **QiNetwork.cs** | MonoBehaviour (Singleton) | Qi power grid manager. BFS connectivity from source through QiConduits. Sets IsPowered on machines in range. Dirty-flag optimization |
| **QiConduit.cs** | MonoBehaviour | Qi power pole. connectionRadius (conduit↔conduit), machineRadius (conduit→machine). IInteractable + IMachineConnectable |
| **MachineInventory.cs** | Class | Shared item buffer (Dictionary<ItemData, int>) with capacity limits. OnChanged event for UI updates |
| **SpiritPipe.cs** | MonoBehaviour | Transport system. Connects output of one machine to input of another. Configurable transferInterval and itemsPerTransfer. Optional item filter. Checks StorageContainer.AcceptsItem() |
| **Splitter.cs** | MonoBehaviour | 1-input / 2-output routing node. Round-robin distribution. Implements IInteractable + IMachineConnectable |
| **Merger.cs** | MonoBehaviour | 2-input / 1-output routing node. Alternately pulls from two sources. Implements IInteractable + IMachineConnectable |
| **OreVein.cs** | MonoBehaviour | World resource node with finite yield, depletion, and respawn. Uses WorldState for persistence. Visual dimming via MaterialPropertyBlock. Implements IInteractable |
| **ResourceExtractor.cs** | MonoBehaviour | Machine that auto-mines nearby OreVeins (Physics.OverlapSphere, 4m radius). Guards against zero processingSpeed. Implements IInteractable + IMachineConnectable |
| **StorageContainer.cs** | MonoBehaviour | Buffer storage between production steps. Single MachineInventory for input+output. Optional item filter via AcceptsItem(). Implements IInteractable + IMachineConnectable |
| **PipeConnectionVisualizer.cs** | MonoBehaviour | Visualizes spirit pipe connections using LineRenderers |
| **SoundManager.cs** | MonoBehaviour (Singleton) | Audio system: SFX pool, music playback, master/SFX/music volume controls |
| **SoundEventTrigger.cs** | MonoBehaviour | Bridges GameEvents/GameDataEvents to SoundManager for automatic audio playback |
| **BreakthroughSystem.cs** | MonoBehaviour | Handles cultivation rank-up: Qi consumption, success/failure rolls, realm advancement |
| **PillBuffSystem.cs** | MonoBehaviour | Pill consumption and buff application. Applies temporary buffs (Meditation, Breakthrough, Damage, Defense, Speed) via CultivationBuffs. Coroutine-based duration tracking |
| **EnemyAI.cs** | MonoBehaviour | NavMesh-based AI: Patrol → Detect → Chase → Attack state machine |
| **EnemyLoot.cs** | MonoBehaviour | Delegates to LootSystem on enemy death to drop loot |
| **CraftingStation.cs** | MonoBehaviour | World-placed crafting table. Implements IInteractable. Opens crafting UI |
| **Portal.cs** | MonoBehaviour | Scene transition portal. Implements IInteractable |
| **RealmPortal.cs** | MonoBehaviour | Realm-specific portal with cultivation requirements |
| **SceneEntryPoint.cs** | MonoBehaviour | Scene entry spawn point for player |
| **SpiritEssence.cs** | MonoBehaviour | World-placed collectible essence. Implements IInteractable. Grants Qi + adds to inventory |
| **BiomeZoneMap.cs** | Class | Biome zone mapping for procedural generation |
| **HeightmapBuilder.cs** | Class | Terrain heightmap generation |
| **MinorRealmGenerator.cs** | MonoBehaviour | Minor realm procedural generation |
| **SaveSystem.cs** | MonoBehaviour | Core save/load logic with atomic JSON (write-to-temp, rename) |
| **SceneTransitionData.cs** | Static class | Cross-scene data passing |
| **WorldState.cs** | Static class | Persistent world state tracking (spawner respawns, ore vein depletion) |

### UI (namespace: CultivationGame.UI)

| Script | Type | Purpose |
|--------|------|---------|
| **MachineCatalogueController.cs** | MonoBehaviour | Grid-based machine catalogue UI. Opens in Build Mode, supports dragging machines to hotbar |
| **HotbarController.cs** | MonoBehaviour | Hotbar slot UI management. Handles slot interactions for equipped machines |
| **BuildMenuDataSource.cs** | Struct | MachineSlotData struct for UI binding (Machine, Name, Icon) |
| **CraftingController.cs** | MonoBehaviour | Crafting panel controller. Displays available recipes, handles crafting |
| **GameStateManager.cs** | MonoBehaviour (Singleton) | Panel state management + cursor lock/unlock. Panel ID system for UI layers |
| **UIManager.cs** | MonoBehaviour | Central UI initialization via BroadcastMessage("InitializeUI") to all UI controllers on same GameObject |
| **HUDController.cs** | MonoBehaviour | HUD management: HP, Qi, Stamina bars, buff icons in BuffContainer |
| **HUDDataSource.cs** | ScriptableObject | HUD data binding with INotifyPropertyChanged. HP/Stamina use ProgressBar, Qi uses LiquidCircle |
| **LiquidCircle.cs** | Custom VisualElement | UIElements custom control: circular progress indicator with fill animation for Qi display |
| **InventoryController.cs** | MonoBehaviour | Inventory panel toggle, item display, input map switching |
| **InventoryDataSource.cs** | Struct | InventorySlotData struct for inventory UI binding |
| **CraftingDataSource.cs** | Struct | RecipeSlotData struct for crafting UI binding |
| **BreakthroughController.cs** | MonoBehaviour | Breakthrough confirmation UI panel |
| **HealthBarUI.cs** | MonoBehaviour | Player health bar display |
| **EnemyHealthBarUI.cs** | MonoBehaviour | Enemy health bar (world-space) |
| **DialogueUI.cs** | MonoBehaviour | Dialogue display with typewriter effect, portrait, choice buttons |
| **MachineInspectUI.cs** | MonoBehaviour | Machine inspection panel: status, progress, recipe dropdown, input/output slots, transfer buttons. Uses GameStateManager panel ID "MachineInspect" |
| **FactoryDashboardUI.cs** | MonoBehaviour | Factory dashboard: Qi network status, machine list, bottleneck detection. Polls QiNetwork.GetAllMachines() |
| **PipeConnectionUI.cs** | MonoBehaviour | Pipe connection HUD: source/dest selection, cancel button |
| **PauseMenuController.cs** | MonoBehaviour | Pause menu logic |
| **ResourcePickupNotificationUI.cs** | MonoBehaviour | Item pickup notifications (+N Item Name) for manual and automated collection |
| **SaveManager.cs** | MonoBehaviour | Save/load orchestration: discovers machines via FindObjectsOfType, serializes to JSON. Uses LayerHelper for machine layer setup on load |

---

## Event Systems

The project uses a **dual event bus** architecture to maintain clean assembly dependencies:

### GameEvents.cs (Core Assembly)
Lightweight events for **player state** — accessible from all assemblies.

| Event | Trigger |
|-------|---------|
| OnQiChanged | Player Qi amount changes |
| OnRealmChanged | Player advances to new cultivation realm |
| OnStaminaChanged | Player stamina changes (sprint, regen) |
| OnInventoryChanged | Player inventory contents change |
| OnMeditationToggled | Player enters/exits meditation |
| OnBuildModeToggled | Build mode toggled on/off (Shift key, any perspective) |
| OnBuildLayerChanged | Build elevation layer changed |
| OnPauseStateChanged | Game paused/unpaused |
| OnPanelStateChanged | UI panel opened/closed |
| OnPlayerDodge | Player performs a dodge roll |
| OnBuffStarted | Pill buff starts (buffName, pillName, duration) |
| OnBuffExpired | Pill buff expires (buffName, pillName) |
| OnBreakthroughConfirmRequested | HUD button triggers breakthrough confirmation |
| OnAttemptBreakthrough | Player confirms breakthrough attempt |
| OnRealmBreakthrough | Breakthrough result (success, realmName) |

### GameDataEvents.cs (Data Assembly)
Data-layer events for **factory and crafting systems** — accessible from Systems and UI.

| Event | Trigger |
|-------|---------|
| OnCraftingStarted | Recipe crafting begins |
| OnCraftingCompleted | Recipe crafting finishes |
| OnCraftingFailed | Recipe crafting fails |
| OnPillConsumed | Player consumes a pill |
| OnPillEffectsApplied | Pill buff effects applied |
| OnMachinePlaced | Machine placed on grid |
| OnMachineRemoved | Machine removed from grid |
| OnBuildModeGhostStarted | Ghost placement preview started |
| OnBuildModeGhostCancelled | Ghost placement preview cancelled |
| OnQiNetworkChanged | Qi network demand/supply changed |
| OnMachineInteracted | Player interacts with a machine |
| OnMachineProcessingCompleted | Machine finishes processing a recipe |
| OnMachineStalled | Machine stalls due to power loss |
| OnPipeConnected | Spirit pipe connection established |
| OnPipeDisconnected | Spirit pipe connection removed |
| OnPipeInteracted | Player interacts with a pipe |
| OnResourceExtracted | Resource extractor mines from ore vein |

---

## Key Data Flows

### Factory Pipeline

```
Player Meditation → Qi Pool
    → QiNetwork (BFS from source through QiConduits)
    → IsPowered = true on machines in conduit range

OreVein (world resource node)
    → ResourceExtractor (auto-mines on timer, Physics.OverlapSphere 4m)
    → SpiritPipe (transfers items at configurable interval)
    → BaseMachine (processes recipe: inputs → outputs on timer, requires IsPowered)
    → SpiritPipe (moves outputs to next stage)
    → StorageContainer (buffer storage)

Splitter (1→2 routing):
    SpiritPipe → Splitter.InputInventory → round-robin to destA.InputInventory / destB.InputInventory

Merger (2→1 routing):
    sourceA.OutputInventory + sourceB.OutputInventory → Merger.OutputInventory → SpiritPipe → next machine
```

### Crafting Flow
```
RecipeData (defines inputs, outputs, processing time)
    + Player Inventory (provides ingredients)
    → CraftingSystem (validates recipe, consumes inputs)
    → Output Items added to Player Inventory
    → fires GameDataEvents.OnCraftingCompleted
```

### Pill Flow
```
PillData (defines tier, qiBoost, buffs, duration)
    → PillBuffSystem.ConsumePill()
    → CultivationBuffs (applies temporary multipliers)
    → PlayerStats (enhanced cultivation speed, breakthrough chance)
    → fires GameDataEvents.OnPillConsumed
```

### Essence Collection (original flow)
```
SpiritEssence (world object)
    → IQiReceiver.AddQi()        → PlayerStats fires OnQiChanged      → HUDController updates
    → IInventory.AddItem()        → PlayerInventory fires OnInventoryChanged
    → Destroy self

```

---

## Unity Editor Setup Guide

### Building System Setup

#### 1. Create a "BuildSystem" GameObject in the Scene

1. In the Hierarchy, create an empty GameObject named **BuildSystem**.
2. Add the **BuildGrid** component.
   - Set **Cell Size** to `2` (2 m × 2 m grid cells).
   - Set **Terrain Layer** to the layer mask that contains your terrain/ground colliders.
3. Add the **PlacementController** component.
   - Drag the **BuildGrid** component into the `Build Grid` field.
   - Drag the **PlayerInventory** (on the Player GameObject) into the `Player Inventory` field.
   - Set **Terrain Layer** to the same terrain layer mask.
   - Set **Build Camera** to the Main Camera (or leave empty — it will fall back to `Camera.main`).
   - Assign the **Input Action References** from the BuildMode map:
     - `Place Action` → `BuildMode/Place`
     - `Cancel Action` → `BuildMode/Cancel`
     - `Rotate Action` → `BuildMode/Rotate`

#### 2. Create Ghost Materials

In `Assets/_Project/Materials/` (create the folder if it doesn't exist):

**GhostValid.mat**
- Shader: **Universal Render Pipeline/Lit**
- Surface Type: **Transparent**
- Base Color: **RGBA (0.2, 0.8, 0.2, 0.4)** (semi-transparent green)

**GhostInvalid.mat**
- Shader: **Universal Render Pipeline/Lit**
- Surface Type: **Transparent**
- Base Color: **RGBA (0.8, 0.2, 0.2, 0.4)** (semi-transparent red)

Assign these to the PlacementController:
- `Ghost Valid Material` → GhostValid
- `Ghost Invalid Material` → GhostInvalid

#### 3. Add Build Menu to the UI

1. Select the **UIManager** GameObject in the scene.
2. Add the **MachineCatalogueController** and **HotbarController** components.
3. Create a **HotbarDataSource** asset:
   - Right-click in Project → **Create → Cultivation → UI → Hotbar Data Source**
   - Save it in `Assets/_Project/Data/UI/`.
4. Wire the HotbarController:
   - `Data Source` → the HotbarDataSource asset you just created.
   - `Placement Controller` → the PlacementController on the BuildSystem GameObject.

#### 4. Create MachineData Assets

For each machine type (Furnace, Crusher, Mixer, etc.):

1. Right-click in Project → **Create → Cultivation → Machine Data**
2. Save in `Assets/_Project/Data/Machines/` (create folder if needed).
3. Fill in:
   - **Machine Name**: e.g. "Furnace"
   - **Machine Type**: select from the enum
   - **Prefab**: the actual machine prefab (create in `Assets/_Project/Prefab/CraftingStations/`)
   - **Ghost Prefab**: (optional) a dedicated ghost prefab, or leave empty to reuse the main prefab
   - **Grid Size**: e.g. (1, 1) for a 2 m × 2 m machine, (2, 1) for a 4 m × 2 m machine
   - **Build Cost**: array of RecipeIngredient (item + amount)
   - **Icon**: a Sprite for the build menu

#### 5. Populate the BuildMenuDataSource

1. Select the BuildMenuDataSource asset.
2. In the **Available Machines** array, add all the MachineData assets you want
   to appear in the build menu.

#### 6. Create Machine Prefabs

In `Assets/_Project/Prefab/CraftingStations/`:

1. Create a prefab for each machine (e.g. a cube placeholder with a collider).
2. Optionally add the `CraftingStation` component (or a new `PlacedMachine` component)
   so the player can interact with placed machines.
3. Make sure each prefab has at least one **Renderer** (for the ghost material swap)
   and one **Collider** (for interaction raycasting after placement).

#### Controls

| Key | Action | Context |
|-----|--------|---------|
| **Left Shift** | Toggle Build Mode | Any perspective |
| Click a machine in the Build Menu | Start ghost placement | Build Mode |
| **Left Click** | Confirm placement | Build Mode (ghost active) |
| **Right Click** or **Escape** | Cancel placement | Build Mode (ghost active) |
| **R** | Rotate ghost 90° | Build Mode (ghost active) |
| **WASD** | Pan camera | Spirit Sense |
| **Middle Mouse + Drag** | Orbit camera | Spirit Sense |
| **Scroll Wheel** | Zoom (realm-based max) | Spirit Sense |
| **Page Up / Page Down** | Change build layer | Spirit Sense |
| **G** | Toggle meditation / Spirit Sense | 3rd Person |

---

### Factory System Setup

#### 1. Create Ore Resources (RawMaterialData assets)
- **Create → Cultivation → Raw Material Data** for each ore type:
  - Spirit Stone Ore (color: cyan/blue)
  - Iron Essence (color: dark gray)
  - Crystal Shard (color: purple)
  - Jade Fragment (color: green)

#### 2. Create OreVeinData assets
- **Create → Cultivation → Ore Vein Data** for each vein type
- Assign the corresponding RawMaterialData to the `resource` field
- Configure `totalYield` (default 100), `yieldPerExtraction` (default 1), `respawnTimeSeconds` (default 600)

#### 3. Create MachineData assets for new machine types
- **Resource Extractor**: MachineType = `ResourceExtractor`, processingSpeed affects extraction interval
- **Storage Container**: MachineType = `Storage`, inputSlots/outputSlots define pipe connections
- **Spirit Pipe**: MachineType = `SpiritPipe`, gridSize = (1,1)

#### 4. Create Prefabs

Each machine type needs a prefab with the appropriate component:

| Machine | Required Component | Notes |
|---------|-------------------|-------|
| Furnace, Crusher, etc. | `BaseMachine` | Needs recipe assignment |
| Resource Extractor | `ResourceExtractor` | Auto-detects nearby OreVeins (4m radius) |
| Storage Container | `StorageContainer` | Single inventory for input+output, optional filter |
| Spirit Pipe | `SpiritPipe` | Connect via PipeConnectionUI |
| Splitter | `Splitter` | 1→2 round-robin distribution |
| Merger | `Merger` | 2→1 stream combining |
| Qi Conduit | `QiConduit` | Qi power distribution |
| Ore Vein | `OreVein` | Needs MeshRenderer + Collider + OreVeinData |

#### 5. Place OreVeins in the Overworld Scene
- Create GameObjects with MeshRenderer + Collider
- Add the `OreVein` component
- Assign an `OreVeinData` asset
- The `uniqueId` auto-generates on first validation

#### 6. Add to Build Menu
- Open the HotbarDataSource in the Inspector
- Machines are available through the MachineCatalogueController in Build Mode

#### How the Factory Loop Works
1. **OreVein** sits in the world with a finite resource yield
2. **ResourceExtractor** placed near an OreVein auto-detects it (Physics.OverlapSphere)
3. Extractor mines on a timer, placing resources in its OutputInventory
4. **SpiritPipe** connects Extractor's output to a BaseMachine's input
5. **BaseMachine** (Furnace, Crusher, etc.) checks inputs against its recipe, processes on timer, produces outputs
6. Another SpiritPipe moves outputs to the next machine or a StorageContainer

#### Spirit Pipe Connection
- Place a Spirit Pipe on the grid
- Interact with it to open PipeConnectionUI
- Select source machine, then destination machine
- Pipe transfers items at `transferInterval` (default 1s), `itemsPerTransfer` (default 1)
- Optional `filterItem` restricts which items flow through
- Checks `StorageContainer.AcceptsItem()` before transferring

#### OreVein Respawn
- Follows the EssenceSpawner pattern using WorldState
- When depleted, records timestamp via `WorldState.RecordSpawnerCollection()`
- On scene load, checks `WorldState.GetRemainingRespawn()` to resume respawn timer
- Visual dimming via MaterialPropertyBlock when depleted

---

## Scene Setup Checklist

1. **Player GameObject**: PlayerStats, PlayerMovement, PlayerInteractor, PlayerInventory, PlayerCombatController, HealthSystem components
2. **UI Document**: UIManager + all UI controllers as components on the same GameObject (MachineCatalogueController, HotbarController, MachineInspectUI, FactoryDashboardUI, PipeConnectionUI, BreakthroughController, HealthBarUI, EnemyHealthBarUI, DialogueUI, CraftingController, InventoryController, PauseMenuController, ResourcePickupNotificationUI, HUDController)
3. **GameStateManager**: Singleton in scene for panel state and cursor management
4. **EventSystem**: Required for UI input (with InputSystemUIInputModule)
5. **BuildSystem**: Empty GameObject with BuildGrid + PlacementController components (see Building System Setup above)
6. **SoundManager**: Singleton in scene + SoundEventTrigger on a nearby GameObject
7. **BreakthroughSystem**: On a GameObject with access to PlayerStats
8. **PillBuffSystem**: On its own GameObject or on Player
9. **PipeConnectionVisualizer**: On its own GameObject
10. **QiNetwork**: Singleton with qiSourcePoint reference
11. **OreVeins**: Place OreVein GameObjects in the overworld with OreVeinData assigned
12. **Machine Prefabs**: Ensure all machine prefabs are registered in the build menu data



---

## Future Work

→ Siehe `Assets/_Project/TODO.md`
